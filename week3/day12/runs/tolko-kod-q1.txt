```js
// read-json.mjs
import { readFile } from 'node:fs/promises';

export async function readJson(file) {
  let raw;
  try {
    raw = await readFile(file, 'utf8');
  } catch (err) {
    if (err.code === 'ENOENT') throw new Error(`File not found: ${file}`);
    if (err.code === 'EACCES') throw new Error(`Permission denied: ${file}`);
    throw err;
  }

  try {
    return JSON.parse(raw);
  } catch (err) {
    throw new Error(`Invalid JSON in ${file}: ${err.message}`);
  }
}

// sync variant: readFileSync from 'node:fs' inside the same try/catch layout
```

```js
import { readJson } from './read-json.mjs';

try {
  const data = await readJson('./config.json');
  console.log(data);
} catch (err) {
  console.error(err.message);
  process.exitCode = 1;
}
```

# ===== stderr (метрики) =====
node.exe : runtime: deepseek-harness-sdk-runtime 0.0.1 · provider=deepseek-official model=deepseek-v4-flash
At C:\Users\user\Projects\ai-challenge\week3\day12\run.ps1:52 char:15
+     $stdout = & node @nodeArgs 2> $errFile
+               ~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    + CategoryInfo          : NotSpecified: (runtime: deepse...epseek-v4-flash:String) [], RemoteException
    + FullyQualifiedErrorId : NativeCommandError
 
ui: in-tty=0 out-tty=0 colors=0 md=0
compress: on (compact at 100k tokens, keep last 50k tokens)
profile: «Только код» (tolko-kod) · ~259 токенов в system prompt · файл: C:\Users\user\AppData\Local\Temp\dsh-temp-veri
fy2-home\user-profiles\tolko-kod.md
(queued cc721a39-eaf2-46ae-bb41-4b9c439853f8)
───────────────────────────────────────────────────────────────────────────────
— turn 1 ended: completed
  tokens: in 8.3k (cache 0) / out 220
  requests: 1 (this turn: 1)
  context: 8.3k / 1M (0.832%)
